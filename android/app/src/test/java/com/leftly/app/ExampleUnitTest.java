package com.leftly.app;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

/** Local unit test for the Leftly application module. */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
}
