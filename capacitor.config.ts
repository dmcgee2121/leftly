import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.leftly.app',
  appName: 'Leftly',
  webDir: 'dist',
  android: {
    backgroundColor: '#050914',
  },
  ios: {
    backgroundColor: '#050914',
  },
}

export default config
